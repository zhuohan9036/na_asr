experiment:
  name: wav2vec2_base_960h_zeroshot_speaker_independent

data:
  test_manifest: data/l2arctic/manifests/speaker_independent/test.jsonl

model:
  name: facebook/wav2vec2-base-960h
  sampling_rate: 16000
  torch_dtype: float32

runtime:
  device: cuda

output:
  prediction_path: results/l2arctic/predictions/wav2vec2_base_960h_zeroshot_speaker_independent.jsonl
  metric_path: results/l2arctic/metrics/wav2vec2_base_960h_zeroshot_speaker_independent.json