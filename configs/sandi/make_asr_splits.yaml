experiment:
  name: sandi_make_asr_splits

seed: 42

data:
  metadata_path: data/sandi/raw/metadata_all.jsonl

split:
  train_ratio: 0.8
  dev_ratio: 0.1
  test_speaker_ratio: 0.1

output:
  manifest_root: data/sandi/manifests
  table_dir: results/sandi/tables